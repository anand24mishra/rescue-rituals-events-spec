# Base URL of the Events API.
#
# Leave this unset in local development: Vite proxies /api to localhost:3000
# (see vite.config.ts), so requests are same-origin and CORS is not involved.
#
# Set it for a deployed build, e.g. VITE_API_BASE_URL="https://your-api.example.com"
VITE_API_BASE_URL=
